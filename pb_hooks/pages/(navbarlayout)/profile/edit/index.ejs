<div class="relative min-h-[calc(100vh-80px)] overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-base-100 to-secondary/10">
        <div
            class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear_gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]">
        </div>
        <div class="absolute inset-0 bg-gradient-to-t from-base-100 via-base-100/50 to-transparent"></div>
        <div
            class="absolute top-1/3 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-secondary/15 rounded-full blur-[120px] opacity-50">
        </div>
    </div>

    <div class="container relative z-10 mx-auto px-4 py-12">
        <div class="card border border-base-content/10 bg-base-100/60 shadow-2xl backdrop-blur-md max-w-2xl mx-auto">
            <div class="card-body p-8">
                <!-- Header -->
                <div class="flex items-center gap-4 mb-6">
                    <div
                        class="flex items-center justify-center w-12 h-12 mask mask-squircle bg-gradient-to-br from-secondary/20 to-primary/20 border border-white/10">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-secondary" viewBox="0 0 20 20"
                            fill="currentColor">
                            <path
                                d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z" />
                        </svg>
                    </div>
                    <div>
                        <h2
                            class="text-2xl font-bold bg-gradient-to-r from-base-content to-base-content/70 bg-clip-text text-transparent">
                            Edit Profile
                        </h2>
                        <p class="text-base-content/50 text-sm">Update your personal information</p>
                    </div>
                </div>

                <div class="divider my-4"></div>

                <form method="POST" enctype="multipart/form-data" class="space-y-6" onsubmit="if (window.isAvatarConverting) { event.preventDefault(); return false; }">
                    <!-- Avatar Section -->
                    <% const hasAvatar = !!data.profile.getString('avatar'); %>
                    <div class="form-control w-full">
                        <div class="flex flex-col items-center gap-3">
                            <div class="avatar placeholder cursor-pointer group relative"
                                onclick="document.getElementById('avatar-input').click()">
                                <div
                                    class="bg-neutral text-neutral-content mask mask-squircle w-28 group-hover:opacity-80 transition-all duration-300 shadow-xl shadow-primary/10">
                                    <div id="avatar-placeholder"
                                        class="text-3xl w-full h-full flex items-center justify-center bg-gradient-to-br from-primary to-secondary text-primary-content font-bold <%= hasAvatar ? 'hidden' : '' %>">
                                        <%= (data.profile.getString('name') || data.profile.email()).slice(0, 1).toUpperCase() %>
                                    </div>
                                    <img id="avatar-preview"
                                        src="<%= hasAvatar ? '/api/files/' + data.profile.collection().id + '/' + data.profile.id + '/' + data.profile.getString('avatar') : '' %>"
                                        alt="Avatar" class="w-full h-full object-cover <%= hasAvatar ? '' : 'hidden' %>" />

                                    <!-- Hover Overlay -->
                                    <div
                                        class="absolute inset-0 bg-black/50 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity mask mask-squircle">
                                        <svg xmlns="http://www.w3.org/2000/svg" fill="none"
                                            viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"
                                            class="w-8 h-8 text-white">
                                            <path stroke-linecap="round" stroke-linejoin="round"
                                                d="M6.827 6.175A2.31 2.31 0 015.186 7.23c-.38.054-.757.112-1.134.175C2.999 7.58 2.25 8.507 2.25 9.574V18a2.25 2.25 0 002.25 2.25h15A2.25 2.25 0 0021.75 18V9.574c0-1.067-.75-1.994-1.802-2.169a47.865 47.865 0 00-1.134-.175 2.31 2.31 0 01-1.64-1.055l-.822-1.316a2.192 2.192 0 00-1.736-1.039 48.774 48.774 0 00-5.232 0 2.192 2.192 0 00-1.736 1.039l-.821 1.316z" />
                                            <path stroke-linecap="round" stroke-linejoin="round"
                                                d="M16.5 12.75a4.5 4.5 0 11-9 0 4.5 4.5 0 019 0zM18.75 10.5h.008v.008h-.008V10.5z" />
                                        </svg>
                                    </div>
                                </div>
                            </div>
                            <div class="flex items-center gap-2">
                                <button type="button" onclick="document.getElementById('avatar-input').click()"
                                    class="btn btn-xs btn-ghost text-base-content/70 hover:bg-base-300 gap-1.5 font-medium">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12" />
                                    </svg>
                                    <span>Upload photo</span>
                                </button>
                                <button type="button" id="remove-avatar-btn" onclick="removeAvatar(event)"
                                    class="btn btn-xs btn-ghost text-error hover:bg-error/10 gap-1.5 font-medium <%= hasAvatar ? '' : 'hidden' %>">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                    </svg>
                                    <span>Remove photo</span>
                                </button>
                            </div>
                        </div>

                        <input id="remove-avatar-input" type="hidden" name="remove_avatar" value="0" />
                        <input id="avatar-input" type="file" name="avatar" class="hidden" accept="image/*"
                            onchange="previewAvatar(this)" />
                    </div>

                    <script src="/js/profile-edit.js?v=<%= Date.now() %>"></script>

                    <!-- Email (disabled) -->
                    <div class="form-control w-full">
                        <label class="label">
                            <span class="label-text font-medium">Email</span>
                        </label>
                        <input type="text" value="<%= data.profile.email() %>"
                            class="input input-bordered w-full bg-base-200/50 cursor-not-allowed" disabled />
                    </div>

                    <!-- Name -->
                    <div class="form-control w-full">
                        <label class="label">
                            <span class="label-text font-medium">Display Name</span>
                        </label>
                        <input type="text" name="name" value="<%= data.profile.getString('name') || '' %>"
                            placeholder="Enter your display name"
                            class="input input-bordered w-full bg-base-100/50 focus:border-secondary focus:ring-2 focus:ring-secondary/20" />
                    </div>

                    <!-- Handle -->
                    <div class="form-control w-full">
                        <label class="label">
                            <span class="label-text font-medium">Handle</span>
                        </label>
                        <input type="text" name="shortHand" value="<%= data.profile.getString('shortHand') || '' %>"
                            placeholder="e.g. moviebuff"
                            class="input input-bordered w-full bg-base-100/50 focus:border-secondary focus:ring-2 focus:ring-secondary/20" />
                    </div>

                    <!-- Bio -->
                    <div class="form-control w-full">
                        <label class="label">
                            <span class="label-text font-medium">Bio</span>
                        </label>
                        <textarea name="bio"
                            class="textarea textarea-bordered h-24 bg-base-100/50 focus:border-secondary focus:ring-2 focus:ring-secondary/20"
                            placeholder="Tell us about yourself..."><%= data.profile.getString('bio') || '' %></textarea>
                    </div>

                    <!-- Created Date (disabled) -->
                    <div class="form-control w-full">
                        <label class="label">
                            <span class="label-text font-medium">Member Since</span>
                        </label>
                        <input type="text" value="<%= (() => {
                            const d = new Date(data.profile.getString('created'));
                            const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
                            return `${months[d.getMonth()]} ${d.getDate()}, ${d.getFullYear()}`;
                        })() %>" class="input input-bordered w-full bg-base-200/50 cursor-not-allowed" disabled />
                    </div>

                    <!-- Actions -->
                    <div class="flex gap-3 pt-4">
                        <a href="/profile" class="btn btn-ghost flex-1 border border-base-content/10">Cancel</a>
                        <button type="submit"
                            class="btn btn-secondary flex-1 gap-2 shadow-lg shadow-secondary/20 hover:scale-[1.02] transition-all duration-300">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20"
                                fill="currentColor">
                                <path fill-rule="evenodd"
                                    d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                                    clip-rule="evenodd" />
                            </svg>
                            Save Changes
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>